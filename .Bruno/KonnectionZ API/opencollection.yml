opencollection: 1.0.0

info:
  name: KonnectionZ API
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ""
      port: ""
      auth:
        username: ""
        password: ""
      bypassProxy: ""

request:
  auth:
    type: oauth2
    flow: resource_owner_password_credentials
    accessTokenUrl: http://localhost:8000/auth/token
    credentials:
      clientId: fsfdsfsd
      placement: body
    resourceOwner:
      username: olle
      password: Sharp/1980!
    scope: user:play user:admin
    tokenConfig:
      id: credentials
      placement:
        header: Bearer
      source: access_token
    settings:
      autoFetchToken: true
      autoRefreshToken: false
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
