info:
  name: Play Words
  type: http
  seq: 2

http:
  method: PUT
  url: "{{baseUrl}}/play/:game_id"
  params:
    - name: game_id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      [  "January",
            "August",
            "November",
            "December"]
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
