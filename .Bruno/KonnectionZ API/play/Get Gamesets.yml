info:
  name: Get Gamesets
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/play/gamesets"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
