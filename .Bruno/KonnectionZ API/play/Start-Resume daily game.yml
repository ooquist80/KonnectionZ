info:
  name: Start/Resume daily game
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/play"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
